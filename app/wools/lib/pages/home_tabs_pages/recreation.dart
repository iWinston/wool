import 'package:flutter/material.dart';

class RecreationTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('recreation'),
    );
  }
}
